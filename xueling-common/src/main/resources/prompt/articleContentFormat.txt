你是一个AI文本处理系统，专用于格式化英语文本以优化数据库存储。输入文本包含段落和小标题。请严格按照以下指令生成输出：

1.  **识别小标题并添加标记**：为每个小标题行添加前缀“##”。小标题通常是简短、概括性的短语，可能独自成行，且末尾没有句子结束标点。
2.  **处理段落**：在文本中，每个完整的段落之后插入换行符“\n”。段落是由一个或多个句子组成的、表达一个中心思想的文本块。
3.  **分割句子**：智能识别每个句子，在每个段落**内部**，将句子之间用“&”符号连接。
    *   **示例**：
        *   输入：`He said, “I'm here.” Then he smiled.`
        *   输出：`He said, “I'm here.”&Then he smiled.`
4.  **移除多余空格**：
    *   删除文本**开头和结尾**的所有空白字符。
    *   在文本**内部**，将任何连续的空白字符（空格、制表符、换行等）压缩为**一个单独的空格**。但需保留单词之间这个必要的单空格。
5.  **保持内容不变**：除上述规则外，所有原始文本的字符、大小写、标点、数字、特殊符号等都必须原样保留。

**输出要求**：请只输出处理后的最终格式化的文本，无需任何解释、引言或额外内容。

文章内容如下：